# X Thread — UX Audit Launch

> **Post time:** Monday–Thursday, 3 PM CET (9 AM US East Coast)
> **Format:** Thread (use X Premium long-form if preferred)
> **Images:** Use Midjourney hero image for tweet 1, before/after screenshots for tweet 3

---

## Tweet 1 (Hook)

I taught my AI coding assistant how to think about design.

One skill file. 12 principles. The UI it generates is unrecognizable from before.

Open source. Works with Claude Code, Cursor, Codex, and Windsurf.

🧵👇

[ATTACH: Midjourney hero image or ux-audit-git-banner1.jpg]

---

## Tweet 2 (The Problem)

The dirty secret of AI-generated UI:

It works. It compiles. It does the thing.

But it looks like a developer who's never opened Figma built it. Because that's exactly what happened — the AI has no design taste.

No hierarchy. No feedback states. No accessibility. No spacing discipline.

---

## Tweet 3 (Before/After)

Here's the same delete confirmation — without the skill vs. with it:

WITHOUT:
```
<dialog>
  <p>Are you sure?</p>
  <button>Yes</button> <button>No</button>
</dialog>
```

WITH:
- Names what's being deleted
- States the consequence
- Action-specific labels
- WCAG AA contrast
- Focus management

[ATTACH: Side-by-side screenshot of the code, or a rendered comparison]

---

## Tweet 4 (What It Is)

UX Audit is a skill file — not a library, not a framework.

Drop it into your AI assistant's context and it rewires how it thinks about interfaces:

→ 12 core design principles (with do/don't code examples)
→ 13 deep-dive reference docs
→ 7 component checklists
→ Two modes: Guide (build it right) + Review (audit what exists)

---

## Tweet 5 (How It Works)

Install in 30 seconds:

```bash
curl -sSL https://raw.githubusercontent.com/narenkatakam/ux-audit/main/install.sh | bash
```

Or just tell your AI:

"I'm building a settings page. Apply the ux-audit skill."

It'll apply hierarchy, spacing, accessibility, and feedback patterns — automatically.

---

## Tweet 6 (The Philosophy)

This isn't "make it pretty."

It's: every button communicates intent. Every error prevents the next one. Every loading state preserves layout. Every color choice passes contrast.

Design taste as a system. Not vibes — rules.

---

## Tweet 7 (Origin + CTA)

I forked a Chinese-language UI/UX guide, rewrote it in English, and expanded it from 800 to 5,000+ lines.

Added: accessibility, responsive design, typography, color systems, forms, UI states, overlays, and structured review workflows.

Apache 2.0. Free forever.

GitHub: github.com/narenkatakam/ux-audit

If you build UI with AI, try it once. You won't go back.

---

## Alt: Single Long-Form Post (X Premium Article)

If you prefer a single post over a thread, combine tweets 1-7 into one X article with the same structure. Use the banner image as the article cover.
